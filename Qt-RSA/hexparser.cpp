#include "hexparser.h"

HexParser::HexParser() {

}

//BigInt HexParser::formHex(const QString &hex) {
//    BigInt res = 0;
//    for (auto i = hex.size() - 1; i >= 0; --i) {
//        res += BigInt(hex[i].digitValue()) * pow(BigInt(0xF), hex.size() - i);
//    }

//    return res;
//}

//QString HexParser::toHex(const BigInt &hex) {
//    BigInt res = 0;
//    for (auto i = hex.size() - 1; i >= 0; --i) {
//        res += BigInt(hex[i].digitValue()) * pow(BigInt(0xF), hex.size() - i);
//    }

//    return res;
//}
